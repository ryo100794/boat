# BOAT RACE AI 懸案・進捗記録

更新日時: 2026-07-18 UTC

## 完了

- 当日ダッシュボードを `src/boatrace_ai/web_dashboard.py` に集約中。`webserver_operational*` への依存は切り離し済み。
- メインダッシュボードからモデル性能レポート `/reports/models` へ遷移できるようにした。
- ダッシュボード上部の日付選択は、当日表示前提として非表示化した。
- 選手カードは選手名優先表示にし、選手名が登番だけの場合は履歴から補完するロジックを入れた。
- 開催していない場は「取得中」ではなく「開催なし」扱いにする方向へ修正済み。
- ダッシュボードの3連単予測は、縦長リストから6場面相当の6x6マトリクス表示へ変更済み。高予測セルは色で強調し、セルクリックでオッズ推移へ連動する。
- 共通SQLite接続に `busy_timeout=30000`、`journal_mode=WAL`、`synchronous=NORMAL` を設定し、WebUI/収集並走時の `database is locked` を軽減した。

## 最新数値

- DB races: 557256
- DB entries: 3318684
- DB odds_snapshots: 5251
- DB prediction_races: 180
- DB result_races: 550083
- DB today_races: 288
- 最新適応型資金運用: ROI 0.7527、損益 -1,434,519円、投資 5,799,672円、払戻 4,365,153円。
- 評価 93,774R / 701日、選択 93,490R、券数 722,332。
- 的中率: ticket 2.94%、race 22.73%。

## 専門エージェント回収結果

- Fermat モデル/特徴量: `no_odds_v8` は LogReg 設定スイープ中心で、特徴量ファミリー別ablationは未整備。v8本評価は top1 約56.42%、3連単Top5 約30.79%。pastlog v7 streaming 実装はあるが、通常backtest/joblib成果物の整理が不足。
- Fermat 資金運用: 最新 `pastlog_v7_adaptive` はROI 0.7527で、no_odds_v8系より悪化。平均約8円/券で実運用制約と合わず、100円単位・実オッズ・買い過ぎ制御へ再設計が必要。
- Plato WebUI/整理: `web_dashboard.py` は段階読み込みとキャッシュ済み。旧 `webserver_operational*` は削除対象。モデル/収集v系は現行プロセス依存があるため、安定名へ移してから削除する。
- Plato 残リスク: 当日基準はJST固定に寄せる必要あり。DBが大きく、`database is locked` の残ログがあるため、長い集計はキャッシュ/別プロセス化を続ける。

- Hume データ収集: 本日DB上は288R行だが実開催/実取得は15場×12R=180R。残108Rは非開催/空ページ由来で欠損扱いしない。津10R `2026-07-18-09-10` のF/返還/不成立は、結果取得済み・3連単評価不可として保存できるよう修正済み。
- Hume 収集リスク: `database is locked` がライブ収集ログに残る。HTTPリトライは最大3回、結果リトライは開始後90分まで60秒、6時間まで300秒だが、特殊結果のパース不足はリトライでは解決しない。過去rawで `2022-06-09` の結果LZHが1日欠けている。
- Hubble 依存解析: 稼働中の入口は `predict_loop_no_odds_v8_modelrank` と `adaptive_odds_loop_safe5_no_odds_v8_modelrank`。旧WebUI `webserver_operational*` は削除可。モデル/収集v系は `result_parser_v2`、`live_safe_patch*`、`features_no_odds_v3`、`modeling_no_odds_v4/v6/v8`、入口ループの順で安定名へ移す。

- Dirac 特殊結果: 津10R `2026-07-18-09-10` は結果ページ取得済みだが3連単評価不能。`race_result_status` により、結果取得済みと3連単評価可能を分離する実装を入れた。
- Dirac 実装方針: `race_results` に `finish_status/rank_label/start_timing_raw/start_flag/is_refund`、`payouts` に `payout_status/refund_yen`、必要なら `race_refunds` を追加。完了判定は `result_complete` と `evaluatable` を分ける。fixtureは `data/raw/pages/20260718/09/10/result-20260718T101614Z.html` と過去TXT `2022/20221130/K221130.TXT` を使う。

- Nietzsche M2/M3回収: 特殊結果は「取得済みだが3連単評価不可」として通常評価から分離する。`raw_files` は台帳だけでなく、ローカルファイル存在・HTTP 200・bytes>0 を有効キャッシュ条件にする。SQLiteロックは共通接続でWAL/busy_timeoutを入れる方針。
- Raman M4/M6回収: `no_odds_v8` は特徴量ファミリー別ablation未整備。`pastlog_v7_adaptive` はROI 0.752655だが、1円単位平均8.03円/券で実運用制約外。次は100円丸め・最低100円・日次/券上限・実オッズ必須/skipを同一foldで比較する。
- Turing M4/M6詳細: 現行ROI悪化は `stake_granularity_yen=1` / `min_stake_yen=1` と払戻実績推定オッズ利用を分けて検証する。比較条件は現行、100円制約のみ、100円+締切前実オッズ必須。ablationは `base_pastlog`、`series_cached`、`series_relative`、`rolling_history` から始める。
- Helmholtz M6実装: 適応型資金運用の既定を100円単位・最低100円へ変更。policy出力を `adaptive_unit_yen` に合わせ、1円単位運用はvalidationで弾く。実オッズ必須/skipは次実装へ境界を定義済み。
- Ohm M6設計回収: 締切前推定時刻以前の最新120点3連単オッズを使い、実オッズがないレースは評価対象外にする方針を確定。
- Sartre M4設計回収: `base_pastlog`、`series_cached`、`series_relative`、`rolling_history` のdrop-one-feature-group ablation境界を確定。
- Russell M6実装: `--require-real-odds` を通常/ストリーミング資金運用バックテストへ追加。実オッズなしはskipし、EV/推定払戻は締切前実オッズから算出。
- Euler M4実装: `modeling_pastlog_v7_stream_hash` に `--drop-feature-groups` と `ablation` サブコマンドを追加し、特徴量グループ別比較を実行可能にした。
- Remote Eval: `/workspace/boat-milestone-e07badb` で実オッズ必須の適応型資金運用バックチェック PID 171290、特徴量ablation PID 171291、資金配分スイープ PID 171523-171527 を起動済み。PID 171523-171525 は疎行列indexエラーで失敗、PID 171526-171527 は継続中。実オッズ必須fold1は全skipで、過去実オッズ履歴不足を確認。

## 改善事項リスト

- M6-1 実オッズ履歴不足: 実オッズ必須バックチェックのfold1は `evaluated_races=0`、`skipped_no_real_odds=18751`。リアルタイムオッズ蓄積が十分になるまでは、過去ログ中心の資金運用評価を主判定にする。進捗25%。
- M6-2 資金配分パラメータ探索: EV閾値、購入点数、Kelly比率、日次/レース/券上限のスイープをリモートで実行中。PID 171523-171525 は疎行列indexエラーで失敗、PID 171526-171527 は継続中。進捗35%。
- M6-3 完了ゲート: ROI 1.0以上、損益プラス、ドローダウン許容、購入日数/的中率の劣化なしを満たすまでM6は完了扱いしない。進捗20%。
- M6-4 特徴量改善の反映: M4 ablation結果を資金運用モデルの入力特徴量と購入判断へ反映する。進捗15%。
- M6-5 疎行列index互換: FeatureHasher出力がint64 indexとなりscikit-learnで失敗したため、学習/推論前にCSR indexをint32へ正規化する。修正同期後に失敗スイープを再実行する。進捗60%。

## 進行中

- 本日分のレース情報、締切時刻、出走時刻、オッズ、結果、モデル予測のリアルタイム更新。
- 過去10年分の公式データ収集。取得順は新しい日付から古い日付へ遡る。
- 過去ログ中心モデルと、リアルタイムデータ併用モデルの並走。
- 資金10000円を前提にした適応型資金運用バックチェック。要改善のため改善事項M6-1..M6-5で追跡中。
- 特徴量チューニングと結果相関分析。特徴量グループablation CLIは実装済みで、SSHリモートで評価ジョブ実行中。
- 残存する v1/v2/v8 などの番号付きモデル/収集ファイルの安定名移行。

## 主要懸案

- 最新の適応型資金運用バックチェックはROIが1.0未満で、購入条件と特徴量の再設計が必要。
- リアルタイムオッズ系列はまだ蓄積途中のため、現時点の主モデルは過去ログ中心に寄せる必要がある。
- 結果待ちが長時間残るケースは、結果取得ループ、公式ページ更新タイミング、記録形式の揺れ、リトライ不足を切り分ける必要がある。津10RのようなF/返還/不成立は、結果取得完了と評価可能を分ける実装で再取得滞留から除外する。
- 欠損に見える箇所は、本当の未取得か、公式テキスト/HTMLの形式差かを再判定する必要がある。
- WebUI以外の v 系ファイルは、稼働中プロセスが依存しているため、安定名へ移してから削除する。
- GPU利用は高次元探索やXGBoost系の短時間探索には有効だが、workspace共有とメモリquotaを前提に分割実行する。

## マイルストーン

- M0: 当日ダッシュボード運用を安定化する。初回表示を軽くし、重い集計は段階読み込みとキャッシュにする。
- M1: 懸案・進捗ページを作り、常時更新される運用ページとして使う。
- M2: 本日分の公式データ取得、締切直前オッズ、結果取得リトライを安定させる。特殊結果の保存/評価分離は実装済み。
- M3: 過去10年バックフィルを、新しい日付から古い日付へ進め、欠損日を再取得する。
- M4: 過去ログ中心モデルの特徴量を見直し、場、枠、選手、モーター、ボート、天候、展示、時系列実績を体系化する。特徴量グループablationをリモート実行中。
- M5: リアルタイム併用モデルをshadow評価で並走させ、十分な蓄積後に本番予測へ昇格する。
- M6: 適応型資金運用モデルを、点数固定/100円固定ではなく期待値・不確実性・日次資金制約で最適化する。要改善。改善事項M6-1..M6-5で管理し、ROI/損益ゲート達成まで完了扱いしない。
- M7: 番号付きファイルを安定名へ移行し、ローカルgit履歴に残したうえで不要ファイルを削除する。

## 専門エージェント分担

- Fermat: モデル/特徴量/バックチェック指標の棚卸し。完了。
- Hume: データ収集/リカバリ/取得失敗と結果待ちの棚卸し。完了。
- Plato: WebUI性能/コード整理/v系ファイル依存の棚卸し。完了。

- Hubble: 番号付きファイル安定名移行の依存解析。完了。
- Dirac: F/返還/不成立など特殊結果パーサ修正方針の調査。完了。
- Nietzsche: M2/M3 の特殊結果、raw欠損再取得、SQLiteロック対策の最小パッチ単位を回収。完了。
- Raman: M4/M6 の特徴量ablation、100円実運用制約、実オッズ評価の最小改修点を回収。完了。
- Singer: rawキャッシュ有効判定を最小実装。`status_code=200`、`bytes>0`、ローカル実ファイル存在時だけskipする。完了。
- Turing: 資金運用制約と特徴量ablationの具体実装点を回収。100円単位/実オッズ必須/欠損skipの3条件比較を次実装へ送る。完了。

- Helmholtz: 資金運用100円単位/最低100円の実装と実オッズ評価境界の回収。完了。
- Linnaeus: 特殊結果の保存/評価分離を実装。津10R fixtureで `final` / `trifecta_evaluable=false` / 再取得候補0件を確認。完了。
- Ohm: 実オッズ必須/skip バックチェック設計を回収。完了。
- Sartre: 特徴量グループablationの最小改修点を回収。完了。
- Russell: 実オッズ必須/skip資金運用モードを実装。完了。
- Euler: 特徴量グループablation CLIを実装。完了。
