# BOAT RACE AI 懸案・進捗記録

更新日時: 2026-07-19 UTC

## 完了

- 当日ダッシュボードを `src/boatrace_ai/web_dashboard.py` に集約中。`webserver_operational*` への依存は切り離し済み。
- メインダッシュボードからモデル性能レポート `/reports/models` へ遷移できるようにした。
- ダッシュボード上部の日付選択は、当日表示前提として非表示化した。
- 選手カードは選手名優先表示にし、選手名が登番だけの場合は履歴から補完するロジックを入れた。
- 開催していない場は「取得中」ではなく「開催なし」扱いにする方向へ修正済み。
- ダッシュボードの3連単予測は、縦長リストから6場面相当の6x6マトリクス表示へ変更済み。高予測セルは色で強調し、セルクリックでオッズ推移へ連動する。
- 共通SQLite接続に `busy_timeout=30000`、`journal_mode=WAL`、`synchronous=NORMAL` を設定し、WebUI/収集並走時の `database is locked` を軽減した。
- 常駐収集・予測は日付引数を省略したJST自動モードへ変更。各ループで運用日を再計算し、日付変更時は公式開催一覧、取得不能時は24場の1Rプローブから開催場だけを展開して当日出走表を初期化する。Webの前日キャッシュはJST日付境界で無効化する。
- SQLite初期化はロック時に上限付き再試行し、日付切替時の並行起動で常駐処理が即終了しないようにした。

## 最新数値

- DB races: 557520
- DB entries: 3319836
- DB odds_snapshots: 9693
- DB prediction_races: 370
- DB result_races: 550096
- DB today_races: 264（有効開催192R）
- 最新適応型資金運用: ROI 0.7527、損益 -1,434,519円、投資 5,799,672円、払戻 4,365,153円。
- 評価 93,774R / 701日、選択 93,490R、券数 722,332。
- 的中率: ticket 2.94%、race 22.73%。

## 専門エージェント回収結果
- 2026-07-19 M4-2/M6評価回収: ROI帰属retry3は93,774R、ROI 0.893462、損益 -112,270円で完了。5foldの安定シグナルは0件のため相関上位の本番採用は見送り、ablationと80foldを継続。
- 2026-07-19 Web/収集復旧: 10001をHTTP/API検証後に更新。タイムラインの締切5分前以降も最新モデル予想・確率・オッズを一括取得し、全行の表示順を締切時刻昇順へ統一。ライブは公式直接プレイヤーURL・出走後10分窓へ変更し、2件以上は2x2四分割で最大4配信を表示。締切後表示は合計4件のまま確定2件を優先。
- 2026-07-19 M2結果優先取得: 出走5分後から未確定結果を通常オッズ処理より先に照会し、30分まで10秒、90分まで30秒、6時間まで120秒で再試行。安定版collector初回で結果対象1件を優先照会、オッズ34件・予測34件・失敗0を確認。
- 2026-07-19 M7完了: 到達22モジュールを安定名へ移行、未到達64モジュールを削除し、package内番号付きPythonは0件。旧joblib互換読込、安定版Web/collector/predictor常駐、30テスト通過を確認。

- 2026-07-19回収: Curieの相関監査retry PID 174501は完了。FisherのROI帰属実装とNoetherのNN shadow設計を回収し、未完の評価作業はM4-1/M4-2/M6へ移管。リモートPIDはエージェント一覧から分離し、モデル性能ページの評価ジョブ表で追跡する。
- 2026-07-19 Web/運用更新: モデル性能ページへ基準1着/3連単Top5、最高/適応型ROI、リモート評価ジョブの状態・fold・経過・ROI・損益を追加。埋め込みHTML 3本をテンプレートへ分離し、ライブは公式直接プレイヤーURLで開始から10分未満だけ表示し、非表示/X時にiframeをabout:blankへ破棄する。
- 2026-07-19 M2更新: 公式日次LZHを一次ソース、公式HTMLを補完ソースとして自動取得。DOMの艇色クラスを基準に6艇を解析し、シリーズ履歴の枠番号混入を回避。JST日付自動切替後の常駐収集で192R/1152艇、オッズ30R、予測30R、取得失敗0を確認。
- 2026-07-19 Web区分更新: 締切後から出走前を「出走待」、出走から5分未満を「出走」、5分以降の未確定を「結果待」と表示。T-5超過は判定/結果列で「締切mm:ss前」を1秒更新し、締切/出列の残時間表示を削除。10001復旧後にHTTP 200と実データの3区分を確認。
- 2026-07-19 M6回収: normalized_kelly 5条件は全完了し、最高EV2.0/日次10券でROI 0.893462、損益 -112,270円。M6-2、M6-5、M6-6は完了。ROI/損益ゲートは未達。旧venv参照で停止したROI帰属はworkspace/srcを明示し、retry3 PID 178254を正しい全引数で再投入し、完了。時間fold安定シグナルは0件。
- Fermat モデル/特徴量: `no_odds_v8` は LogReg 設定スイープ中心で、特徴量ファミリー別ablationは未整備。v8本評価は top1 約56.42%、3連単Top5 約30.79%。pastlog v7 streaming 実装はあるが、通常backtest/joblib成果物の整理が不足。
- Fermat 資金運用: 最新 `pastlog_v7_adaptive` はROI 0.7527で、no_odds_v8系より悪化。平均約8円/券で実運用制約と合わず、100円単位・実オッズ・買い過ぎ制御へ再設計が必要。
- Plato WebUI/整理: `web_dashboard.py` は段階読み込みとキャッシュ済み。旧WebUIと番号付きモデル/収集モジュールの整理は完了。安定版入口と旧joblib互換を運用監視する。
- Plato 残リスク: 当日基準はJST固定に寄せる必要あり。DBが大きく、`database is locked` の残ログがあるため、長い集計はキャッシュ/別プロセス化を続ける。

- Hume データ収集: 本日DB上は264R行、実開催/実取得は16場×12R=192R。残72Rは非開催/空ページ由来で欠損扱いしない。津10R `2026-07-18-09-10` のF/返還/不成立は、結果取得済み・3連単評価不可として保存できるよう修正済み。
- Hume 収集リスク: `database is locked` がライブ収集ログに残る。HTTPリトライは最大3回、結果リトライは出走5分後から30分まで10秒、90分まで30秒、6時間まで120秒だが、特殊結果のパース不足はリトライでは解決しない。過去rawで `2022-06-09` の結果LZHが1日欠けている。
- Hubble 依存解析: 稼働中の入口は `realtime_predictor` と `realtime_collector`。安定名移行と未到達ファイル削除は完了。`realtime_predictor` と `realtime_collector` の常駐、旧joblib互換、番号付き0件を継続監視する。

- Dirac 特殊結果: 津10R `2026-07-18-09-10` は結果ページ取得済みだが3連単評価不能。`race_result_status` により、結果取得済みと3連単評価可能を分離する実装を入れた。
- Dirac 実装方針: `race_results` に `finish_status/rank_label/start_timing_raw/start_flag/is_refund`、`payouts` に `payout_status/refund_yen`、必要なら `race_refunds` を追加。完了判定は `result_complete` と `evaluatable` を分ける。fixtureは `data/raw/pages/20260718/09/10/result-20260718T101614Z.html` と過去TXT `2022/20221130/K221130.TXT` を使う。

- Nietzsche M2/M3回収: 特殊結果は「取得済みだが3連単評価不可」として通常評価から分離する。`raw_files` は台帳だけでなく、ローカルファイル存在・HTTP 200・bytes>0 を有効キャッシュ条件にする。SQLiteロックは共通接続でWAL/busy_timeoutを入れる方針。
- Raman M4/M6回収: `no_odds_v8` は特徴量ファミリー別ablation未整備。`pastlog_v7_adaptive` はROI 0.752655だが、1円単位平均8.03円/券で実運用制約外。次は100円丸め・最低100円・日次/券上限・実オッズ必須/skipを同一foldで比較する。
- Turing M4/M6詳細: 現行ROI悪化は `stake_granularity_yen=1` / `min_stake_yen=1` と払戻実績推定オッズ利用を分けて検証する。比較条件は現行、100円制約のみ、100円+締切前実オッズ必須。ablationは `base_pastlog`、`series_cached`、`series_relative`、`rolling_history` から始める。
- Helmholtz M6実装: 適応型資金運用の既定を100円単位・最低100円へ変更。policy出力を `adaptive_unit_yen` に合わせ、1円単位運用はvalidationで弾く。実オッズ必須/skipは次実装へ境界を定義済み。
- Ohm M6設計回収: 締切前推定時刻以前の最新120点3連単オッズを使い、実オッズがないレースは評価対象外にする方針を確定。
- Sartre M4設計回収: `base_pastlog`、`series_cached`、`series_relative`、`rolling_history` のdrop-one-feature-group ablation境界を確定。
- Russell M6実装: `--require-real-odds` を通常/ストリーミング資金運用バックテストへ追加。実オッズなしはskipし、EV/推定払戻は締切前実オッズから算出。
- Euler M4実装: `feature_tuning` に `--drop-feature-groups` と `ablation` サブコマンドを追加し、特徴量グループ別比較を実行可能にした。
- Remote Eval: `/workspace/boat-milestone-e07badb` で疎行列index修正を同期し、固定版ジョブを再投入。旧資金配分スイープ PID 171806-171810 はfold1で候補あり/選択0件となったため停止。日次上位候補制限と `normalized_kelly` 配分を追加し、正規化Kellyスイープ PID 172555-172559 と短縮sanity PID 172873 を起動。実オッズ必須 PID 171805は完了、特徴量ablation PID 171811は継続。相関診断 PID 173485 は最終集計の未定義関数で停止したため修正し、retry PID 174501を起動。ROI帰属retry3 PID 178254は完了し、stable_signals=0。80fold sanity PID 172873と特徴量ablation PID 171811を継続する。失敗履歴も監視JSONに残す。`scripts/update_remote_eval_status.py` が `data/remote_eval_status.json` に状態を同期し、懸案ページのリモート評価表へ表示する。
- M6 Dynamic Gate: 懸案ページに完了ゲート表を追加。現時点のbest ROIは `bankroll_backtest_no_odds_v8_10000.json` のROI 0.783/損益 -1,521,400円、最新適応型はROI 0.753/損益 -1,434,519円のためM6は未達。ローカル成果物に加え、`data/remote_eval_status.json` 内のリモート資金運用結果指標も自動判定へ取り込む。正規化Kellyスイープ結果で置換待ち。

## 改善事項リスト

- M4-1 NN shadowモデル導入: 既存の過去ログ中心モデルを主系に残し、選手/場/モーター/ボートのembedding、天候/展示/時系列実績、オッズ系列branchを持つNNを別系統で短縮学習する。資金運用バックチェックでROI/損益/ドローダウンが主系を上回るまで本番昇格しない。進捗10%。
- M4-2 相関監査とROI接続: 特徴量ファミリー別相関、欠損疑似相関、係数ズレをJSONとモデルレポートに追加。さらに選択券だけを場/R帯/枠/確率/EV/オッズ/選手/モーター/ボート/節間実績別に集約し、投資・払戻・損益・ROIを接続した。100券/投資1万円以上を最低証拠量とし、各bucketの同一特徴内の残り券に対するROI差が、5fold中3fold以上・方向一致75%以上・差5ポイント以上で再現するかを判定する。ROI帰属retry3まで回収済みだが、時間fold安定シグナルは0件。相関上位を直接採用せず、ablationと80foldで再探索する。進捗85%。
- M6-1 実オッズ履歴不足: 実オッズ必須バックチェックのfold1は `evaluated_races=0`、`skipped_no_real_odds=18751`。リアルタイムオッズ蓄積が十分になるまでは、過去ログ中心の資金運用評価を主判定にする。進捗25%。
- M6-2 資金配分パラメータ探索: 正規化Kelly 5条件を全期間で回収。最高ROI 0.893462、損益 -112,270円。探索工程は完了、収益ゲート未達はM6-3で継続。進捗100%。
- M6-3 完了ゲート: ROI 1.0以上、損益プラス、ドローダウン許容、購入日数/的中率の劣化なしを満たすまでM6は完了扱いしない。懸案ページの完了ゲート表で動的判定する。進捗35%。
- M6-4 特徴量改善の反映: 選手/場/モーター/ボート/節間実績と購入判断軸のROI帰属を資金運用出力へ実装。ROI帰属retry3の時間fold安定シグナルは0件。ablation完了後、特徴群の除外候補を同一foldで再評価する。進捗40%。
- M6-5 疎行列index互換: CSR indexをint32へ正規化後、正規化Kelly 5条件が同エラーなしで完走。完了。進捗100%。
- M6-6 候補あり選択0件の解消: `normalized_kelly` 5条件すべてでselected_tickets>0を確認。完了。80fold sanity PID 172873は再現性監査として継続。進捗100%。

## 進行中

- 本日分のレース情報、締切時刻、出走時刻、オッズ、結果、モデル予測のリアルタイム更新。
- 過去10年分の公式データ収集。取得順は新しい日付から古い日付へ遡る。
- 過去ログ中心モデルと、リアルタイムデータ併用モデルの並走。NN shadowモデルは短縮学習から開始し、バックチェックで主系比較する。
- 資金10000円を前提にした適応型資金運用バックチェック。要改善のため改善事項M6-1..M6-6で追跡中。
- 特徴量チューニングと結果相関分析。特徴量グループablation CLI、ファミリー別/疑似相関診断JSON、選択券ROI帰属、fold間再現性判定を実装済み。ROI帰属は回収済みでstable=0。ablationと80foldの回収をM4-2として継続。
- 番号付きモデル/収集ファイルは22本を安定名へ移行し、未到達64本を削除済み。joblib旧名互換を維持して安定版常駐プロセスへ切替完了。

## 主要懸案

- 最新の適応型資金運用バックチェックはROIが1.0未満。勝者相関だけでは足りないため選択券ROI帰属を実装したが、リモート全期間ROI帰属は回収済みだがstable=0で、採用特徴の変更根拠は未確定。
- リアルタイムオッズ系列はまだ蓄積途中のため、現時点の主モデルは過去ログ中心に寄せる必要がある。
- 結果待ちが長時間残るケースは、結果取得ループ、公式ページ更新タイミング、記録形式の揺れ、リトライ不足を切り分ける必要がある。津10RのようなF/返還/不成立は、結果取得完了と評価可能を分ける実装で再取得滞留から除外する。
- 欠損に見える箇所は、本当の未取得か、公式テキスト/HTMLの形式差かを再判定する必要がある。
- 番号付きpackage Pythonファイルは0件。安定版Web/収集/予測プロセスの常駐と旧joblib互換読込を確認済み。
- GPU利用は高次元探索やXGBoost系の短時間探索には有効だが、workspace共有とメモリquotaを前提に分割実行する。

## マイルストーン

- M0: 当日ダッシュボード運用を安定化する。初回表示を軽くし、重い集計は段階読み込みとキャッシュにする。
- M1: 懸案・進捗ページを作り、常時更新される運用ページとして使う。小フォント高密度表示、改善事項、リモート評価、動的完了ゲートを実装済み。
- M2: 本日分の公式データ取得、締切直前オッズ、結果取得リトライを安定させる。特殊結果の保存/評価分離は実装済み。
- M3: 過去10年バックフィルを、新しい日付から古い日付へ進め、欠損日を再取得する。
- M4: 過去ログ中心モデルの特徴量を見直し、場、枠、選手、モーター、ボート、天候、展示、時系列実績を体系化する。特徴量グループablationをリモート実行中。NN shadowモデルも同じ資金運用バックチェックで比較する。
- M5: リアルタイム併用モデルをshadow評価で並走させ、十分な蓄積後に本番予測へ昇格する。
- M6: 適応型資金運用モデルを、点数固定ではなく期待値・不確実性・100円単位・日次資金制約で最適化する。要改善。改善事項M6-1..M6-6で管理し、ROI/損益/fold再現性ゲート達成まで完了扱いしない。
- M7: 番号付き22本を安定名へ移行し、未到達64本を削除。package内の番号付きPythonは0件で、安定版Web/収集/予測の常駐と旧joblib互換読込を検証済み。

## 専門エージェント分担

- Fermat: モデル/特徴量/バックチェック指標の棚卸し。完了。
- Hume: データ収集/リカバリ/取得失敗と結果待ちの棚卸し。完了。
- Plato: WebUI性能/コード整理/v系ファイル依存の棚卸し。完了。

- Hubble: 番号付きファイル安定名移行の依存解析。完了。
- Dirac: F/返還/不成立など特殊結果パーサ修正方針の調査。完了。
- Nietzsche: M2/M3 の特殊結果、raw欠損再取得、SQLiteロック対策の最小パッチ単位を回収。完了。
- Raman: M4/M6 の特徴量ablation、100円実運用制約、実オッズ評価の最小改修点を回収。完了。
- Singer: rawキャッシュ有効判定を最小実装。`status_code=200`、`bytes>0`、ローカル実ファイル存在時だけskipする。完了。
- Turing: 資金運用制約と特徴量ablationの具体実装点を回収。100円単位/実オッズ必須/欠損skipの3条件比較を次実装へ送る。完了。

- Helmholtz: 資金運用100円単位/最低100円の実装と実オッズ評価境界の回収。完了。
- Linnaeus: 特殊結果の保存/評価分離を実装。津10R fixtureで `final` / `trifecta_evaluable=false` / 再取得候補0件を確認。完了。
- Ohm: 実オッズ必須/skip バックチェック設計を回収。完了。
- Sartre: 特徴量グループablationの最小改修点を回収。完了。
- Russell: 実オッズ必須/skip資金運用モードを実装。完了。
- Euler: 特徴量グループablation CLIを実装。完了。
- Fisher: 選択券の特徴量bucket別投資/払戻/ROIとfold間再現性ゲートを実装して回収。リモート全期間評価はstable_signals=0で回収し、再設計をM4-2/M6へ移管。完了。
